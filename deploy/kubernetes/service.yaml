apiVersion: v1
kind: Service
metadata:
  name: claude-memory
  namespace: claude-memory
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: claude-memory
